
.DS_Store
# V49 Checkpoint D — bloque tous les .env* sauf .env.example (template public).
# Avant V49 : .env.production etait tracked en git car la regle ne le couvrait pas.
.env*
!.env.example
.vite
*-service-account.json
*.log
# Service Account Firebase (NE JAMAIS COMMITER)
firebase-admin-sdk.json
*.serviceaccount.json
*-firebase-adminsdk-*.json
# Backups locaux (jamais versionnés — fichiers de sécurité avant migrations/imports)
backups/
# Build
# Clé privée Firebase Admin — NE JAMAIS COMMITTER
# Dépendances
# Logs
# OS
# Variables d'environnement — NE JAMAIS COMMITTER
build/
dist
dist/
node_modules
node_modules/
npm-debug.log*
service-account.json
Thumbs.db
.vercel
scripts/serviceAccountKey.json

# Scripts temporaires audit/nettoyage
scripts/nettoyage-catalogue.js
RAPPORT-NETTOYAGE-*.TXT
backup-products-*.json

# Audit partenaires
scripts/audit-partenaires.js
AUDIT-PARTENAIRES-*.md
AUDIT-SYSTEME-PARTENAIRES-*.md

# Fichiers d'extraction temporaires (pour analyse externe Gemini)
EXTRACT-COMPLET-*.md
FULL_PROJECT_SNAPSHOT_*.md

# Catalogues Excel (binaires, données métier sensibles)
data/*.xlsx
data/*.xls
data/*.csv
!data/.gitkeep
