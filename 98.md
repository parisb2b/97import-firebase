# 97IMPORT.COM — CONSTRUCTION FROM SCRATCH — BRANCHE V2
# Fichier : 98.md — Version finale avec bypass total
# Basé sur : 2050-prompt-97IMPORT.txt + FIREBASE--import2050-59f11.txt + v5.txt

---

## 🔴 RÈGLE N°1 — STRATÉGIE DOUBLE BRANCHE (IMMUABLE)

```
main  = production actuelle www.97import.com = INTOUCHABLE — ne jamais modifier
v2    = tout le nouveau développement se passe ici uniquement
```

- Firebase projet actuel (main) → NE PAS TOUCHER
- Firebase projet v2 : **`import2050-59f11`** (nouveau projet isolé)
- Vercel : URL de preview auto sur branche v2 (le domaine 97import.com reste sur main)
- Bascule domaine → uniquement quand v2 est 100% validé par Michel

---

## 🔴 RÈGLE N°2 — BYPASS TOTAL (AUCUNE CONFIRMATION)

**Tu n'as le droit de poser AUCUNE question. Tu n'attends AUCUNE confirmation.**
- Erreur rencontrée → tu la corriges seul et tu continues
- Fichier manquant → tu le crées
- Doute sur une décision → tu choisis la meilleure option et tu continues
- Tu ne t'arrêtes jamais jusqu'à ce que toutes les phases A, B, C soient terminées

---

## ⚡ RESET + LANCEMENT — COMMANDES À COPIER DANS LE TERMINAL

```bash
# Ouvrir un nouveau terminal propre et coller tout ce bloc :

cd $(find /Users/mactell -name "package.json" -maxdepth 4 2>/dev/null | grep -v node_modules | head -1 | xargs dirname)

# Afficher le répertoire courant pour confirmation
echo "PROJET: $(pwd)"

# Créer settings.json pour bypass total Claude Code
mkdir -p .claude
cat > .claude/settings.json << 'EOF'
{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Read(*)",
      "Write(*)",
      "Edit(*)",
      "MultiEdit(*)",
      "NotebookEdit(*)"
    ],
    "deny": []
  }
}
EOF

# Sauvegarder les templates Excel avant reset
mkdir -p /tmp/97import-backup-$(date +%Y%m%d)
cp src/templates/*.xlsx /tmp/97import-backup-$(date +%Y%m%d)/ 2>/dev/null && echo "Templates sauvegardés" || echo "Pas de templates à sauvegarder"
cp public/luxent.png /tmp/97import-backup-$(date +%Y%m%d)/ 2>/dev/null || true
cp public/images/* /tmp/97import-backup-$(date +%Y%m%d)/ 2>/dev/null || true

# Revenir sur main propre
git checkout main
git fetch origin && git reset --hard origin/main

# Supprimer branche v2 si elle existe déjà (reset complet)
git branch -D v2 2>/dev/null || true
git push origin --delete v2 2>/dev/null || true

# Créer branche v2 fraîche
git checkout -b v2

# Supprimer tout l'ancien code dans v2
rm -rf src/ public/ functions/ index.html vite.config.ts tsconfig.json package.json tailwind.config.js postcss.config.js 2>/dev/null

# Restaurer les templates sauvegardés
mkdir -p src/templates public/images
cp /tmp/97import-backup-$(date +%Y%m%d)/*.xlsx src/templates/ 2>/dev/null || true
cp /tmp/97import-backup-$(date +%Y%m%d)/luxent.png public/ 2>/dev/null || true
cp /tmp/97import-backup-$(date +%Y%m%d)/*.png public/images/ 2>/dev/null || true

# Copier 98.md à la racine (depuis Downloads)
cp ~/Downloads/98.md . 2>/dev/null || echo "ATTENTION : copier 98.md depuis Downloads manuellement"

# Vérifications
echo "=== BRANCHE ACTIVE ===" && git branch
echo "=== FICHIER 98.md ===" && ls 98.md
echo "=== TEMPLATES ===" && ls src/templates/ 2>/dev/null || echo "vide"

# Lancer Claude Code en bypass total
claude --dangerously-skip-permissions "Lis UNIQUEMENT le fichier 98.md à la racine du projet. Ignore tout autre fichier MD. Tu es sur la branche v2. Ne touche JAMAIS à main. Construis la plateforme 97import.com from scratch selon les instructions de 98.md. Firebase projet import2050-59f11. Aucune confirmation. Aucune question. Exécute les phases A, B, C jusqu'au bout."
```

---

## 🔧 RÈGLES GIT — BRANCHE V2 UNIQUEMENT

```bash
# Après chaque phase :
git add -A
git commit -m "v2 — Phase [X] — [description]"
git push origin v2     # ← TOUJOURS v2, JAMAIS main

# Vérification si doute :
git branch             # doit afficher : * v2
```

---

---

## 🎯 MISSION & RÔLE

**Rôle :** Ingénieur Full-Stack Senior & Architecte Cloud.

**Mission :** Construire de A à Z la plateforme **97import.com** — B2B import Chine → DOM-TOM (Martinique, Guadeloupe, Réunion, Guyane). Produits : mini-pelles, maisons modulaires, solaire, machines agricoles.

**Règles absolues :**
- FROM SCRATCH : ne copier, ne réutiliser, ne lire aucun fichier existant dans src/
- Autonomie totale — zéro confirmation — zéro interruption
- Corriger les erreurs seul et continuer
- Logger chaque action dans MAJALL.TXT à la racine
- Après chaque phase : `git add -A && git commit -m "..." && git push origin main`

---

## 🔧 ÉTAPE 0 — INITIALISATION PROJET

```bash
# Supprimer l'ancien code
rm -rf src/ public/ functions/ index.html vite.config.ts tsconfig.json package.json tailwind.config.js postcss.config.js 2>/dev/null

# Créer le projet Vite from scratch
npm create vite@latest . -- --template react-ts --force
npm install

# Installer toutes les dépendances
npm install firebase wouter jspdf xlsx date-fns
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# Installer shadcn/ui
npx shadcn@latest init --defaults

# Créer le .env.local (clés API — jamais dans Git)
cat > .env.local << 'EOF'
VITE_FIREBASE_API_KEY=AIzaSyCeKBBsSgC8PQK40ETlsjNRwhYCmAKz6cwA
VITE_FIREBASE_AUTH_DOMAIN=import2050-59f11.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=import2050-59f11
VITE_FIREBASE_STORAGE_BUCKET=import2050-59f11.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=496161620887
VITE_FIREBASE_APP_ID=1:496161620887:web:5cdbd6f3a879edd5bfbad2
VITE_FIREBASE_MEASUREMENT_ID=G-7DXCQF6BM0
VITE_DEEPL_KEY=3fae7c40-bed9-48ee-88d9-26c5f719caf3:fx
VITE_EXCHANGE_KEY=51273802828592038c41e0f1
EOF

# Ajouter .env.local au .gitignore
echo ".env.local" >> .gitignore

# Créer MAJALL.TXT
echo "[$(date '+%Y-%m-%d %H:%M')] INIT — Projet créé from scratch" > MAJALL.TXT
```

---

## 🌐 STACK TECHNIQUE IMMUABLE

| Composant | Technologie |
|-----------|-------------|
| Frontend | React 19 + Vite + TypeScript strict |
| UI | shadcn/ui (Radix UI) + Tailwind CSS |
| Routing | wouter |
| Backend | Firebase Auth + Firestore + Storage |
| Fonctions | Firebase Cloud Functions (Node.js) |
| Emails | Resend API via Cloud Functions |
| PDF | jsPDF |
| Excel | xlsx (SheetJS) |
| Déploiement | Vercel — 2 sous-domaines séparés |

**Sous-domaines Vercel :**
- `www.97import.com` → front-office
- `admin.97import.com` → back-office

---

## 🔑 FIREBASE — Projet import2050-59f11

```typescript
// src/lib/firebase.ts
import { initializeApp } from 'firebase/app';
import { getAuth } from 'firebase/auth';
import { getFirestore } from 'firebase/firestore';
import { getStorage } from 'firebase/storage';

const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  storageBucket: import.meta.env.VITE_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: import.meta.env.VITE_FIREBASE_MESSAGING_SENDER_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID,
};

// Instance FRONT (clients)
const clientApp = initializeApp(firebaseConfig, 'client');
export const clientAuth = getAuth(clientApp);
export const db = getFirestore(clientApp);
export const storage = getStorage(clientApp);

// Instance ADMIN (back-office — session isolée)
const adminApp = initializeApp(firebaseConfig, 'admin');
export const adminAuth = getAuth(adminApp);
```

---

## 🌍 INTERNATIONALISATION TRILINGUE

**Langues :** Français (FR) | 中文 (ZH) | English (EN)
**Défaut :** FR
**Aucun texte en dur dans les composants — tout passe par `t('clé')`**

```typescript
// src/i18n/index.ts
import { createContext, useContext, useState } from 'react';

export type Lang = 'fr' | 'zh' | 'en';

export const translations: Record<Lang, Record<string, string>> = {
  fr: {
    'nav.catalogue': 'Catalogue',
    'nav.devis': 'Devis',
    'nav.factures': 'Factures',
    'nav.conteneurs': 'Conteneurs',
    'nav.sav': 'SAV',
    'nav.partenaires': 'Partenaires',
    'nav.produits': 'Produits',
    'nav.clients': 'Clients',
    'nav.taux': 'Taux RMB',
    'nav.logs': 'Logs',
    'nav.parametres': 'Paramètres',
    'sort.recent': 'Plus récents',
    'sort.oldest': 'Plus anciens',
    'btn.dupliquer': 'Dupliquer',
    'btn.nouveau': 'Nouveau',
    'btn.enregistrer': 'Enregistrer',
    'btn.annuler': 'Annuler',
    'btn.encaisser': 'Encaisser',
    'btn.generer': 'Générer',
    'champ.manquant': 'Champ manquant',
    'prix.non.disponible': 'Connectez-vous pour voir les prix',
    'categorie.mini-pelles': 'Mini-Pelles',
    'categorie.maisons-modulaires': 'Maisons Modulaires',
    'categorie.solaire': 'Solaire',
    'categorie.machines-agricoles': 'Machines Agricoles',
    'categorie.divers': 'Divers',
    'categorie.services': 'Services',
    'statut.brouillon': 'Brouillon',
    'statut.envoye': 'Envoyé',
    'statut.accepte': 'Accepté',
    'statut.refuse': 'Refusé',
    'statut.annule': 'Annulé',
  },
  zh: {
    'nav.catalogue': '产品目录',
    'nav.devis': '报价单',
    'nav.factures': '发票',
    'nav.conteneurs': '集装箱',
    'nav.sav': '售后服务',
    'nav.partenaires': '合作伙伴',
    'nav.produits': '产品',
    'nav.clients': '客户',
    'nav.taux': '汇率',
    'nav.logs': '日志',
    'nav.parametres': '设置',
    'sort.recent': '最新',
    'sort.oldest': '最旧',
    'btn.dupliquer': '复制',
    'btn.nouveau': '新建',
    'btn.enregistrer': '保存',
    'btn.annuler': '取消',
    'btn.encaisser': '收款',
    'btn.generer': '生成',
    'champ.manquant': '缺少字段',
    'prix.non.disponible': '请登录查看价格',
    'categorie.mini-pelles': '微型挖掘机',
    'categorie.maisons-modulaires': '模块化房屋',
    'categorie.solaire': '太阳能',
    'categorie.machines-agricoles': '农业机械',
    'categorie.divers': '其他',
    'categorie.services': '服务',
    'statut.brouillon': '草稿',
    'statut.envoye': '已发送',
    'statut.accepte': '已接受',
    'statut.refuse': '已拒绝',
    'statut.annule': '已取消',
  },
  en: {
    'nav.catalogue': 'Catalogue',
    'nav.devis': 'Quotes',
    'nav.factures': 'Invoices',
    'nav.conteneurs': 'Containers',
    'nav.sav': 'After-Sales',
    'nav.partenaires': 'Partners',
    'nav.produits': 'Products',
    'nav.clients': 'Clients',
    'nav.taux': 'RMB Rate',
    'nav.logs': 'Logs',
    'nav.parametres': 'Settings',
    'sort.recent': 'Most Recent',
    'sort.oldest': 'Oldest First',
    'btn.dupliquer': 'Duplicate',
    'btn.nouveau': 'New',
    'btn.enregistrer': 'Save',
    'btn.annuler': 'Cancel',
    'btn.encaisser': 'Collect Payment',
    'btn.generer': 'Generate',
    'champ.manquant': 'Missing field',
    'prix.non.disponible': 'Log in to see prices',
    'categorie.mini-pelles': 'Mini Excavators',
    'categorie.maisons-modulaires': 'Modular Homes',
    'categorie.solaire': 'Solar',
    'categorie.machines-agricoles': 'Agricultural Machinery',
    'categorie.divers': 'Various',
    'categorie.services': 'Services',
    'statut.brouillon': 'Draft',
    'statut.envoye': 'Sent',
    'statut.accepte': 'Accepted',
    'statut.refuse': 'Refused',
    'statut.annule': 'Cancelled',
  },
};

const I18nContext = createContext<{
  lang: Lang; setLang: (l: Lang) => void; t: (k: string) => string;
}>({ lang: 'fr', setLang: () => {}, t: (k) => k });

export const I18nProvider = ({ children }: { children: React.ReactNode }) => {
  const [lang, setLang] = useState<Lang>('fr');
  const t = (key: string) => translations[lang][key] ?? key;
  return <I18nContext.Provider value={{ lang, setLang, t }}>{children}</I18nContext.Provider>;
};

export const useI18n = () => useContext(I18nContext);
```

---

## 🎨 COULEURS BACK-OFFICE (ADMIN_COLORS — immuables)

```css
/* src/admin/styles/admin.css */
:root {
  --navy:         #1E3A5F;   /* sidebar, headers principaux */
  --green:        #166534;   /* succès, validé, encaissé */
  --orange:       #EA580C;   /* alerte, pastille champ manquant */
  --purple:       #7C3AED;   /* VIP, prix négocié, partenaire */
  --gray:         #6B7280;   /* texte secondaire */
  --salmon:       #C87F6B;   /* titres PDF, accents documents */
  --salmon-light: #FBF0ED;   /* fond entêtes tableaux PDF */
  --border:       #E5E5E5;   /* bordures légères */
}
```

---

## 🔑 COMPTES DE TEST (à créer via script au démarrage)

**Mot de passe universel : 20262026**

```javascript
// scripts/seed-users.js — exécuter avec : node scripts/seed-users.js
const { initializeApp, cert } = require('firebase-admin/app');
const { getAuth } = require('firebase-admin/auth');
const { getFirestore } = require('firebase-admin/firestore');

// Requiert serviceAccountKey.json dans scripts/
initializeApp({ credential: cert('./scripts/serviceAccountKey.json') });
const auth = getAuth();
const db = getFirestore();

const USERS = [
  { email: 'admin@97import.com',      role: 'admin',   name: 'Admin 97import' },
  { email: 'client@97import.com',     role: 'user',    name: 'Jean Dupont' },
  { email: 'vip@97import.com',        role: 'vip',     name: 'Marie Martin' },
  { email: 'partenaire@97import.com', role: 'partner', name: 'Pierre Bernard' },
  { email: 'client2@97import.com',    role: 'user',    name: 'Sophie Leblanc' },
];

async function seedUsers() {
  for (const u of USERS) {
    try {
      const existing = await auth.getUserByEmail(u.email).catch(() => null);
      if (existing) await auth.deleteUser(existing.uid);
      const created = await auth.createUser({ email: u.email, password: '20262026', displayName: u.name });
      await auth.setCustomUserClaims(created.uid, { role: u.role });
      await db.collection('profiles').doc(created.uid).set({
        email: u.email, role: u.role, nom: u.name,
        createdAt: new Date().toISOString()
      });
      console.log(`✅ ${u.email} (${u.role}) créé`);
    } catch (e) { console.error(`❌ ${u.email}:`, e.message); }
  }
}
seedUsers();
```

---

## 🗄️ STRUCTURE FIRESTORE COMPLÈTE

```
/products/{id}
  numero_interne      string    "MP-R22-001"
  categorie           string    "mini-pelles"
  sous_categorie      string    "R22"
  nom_fr              string
  nom_zh              string
  nom_en              string
  description_fr      string
  description_zh      string
  description_en      string
  prix_achat_cny      number    Prix Yuan
  prix_achat_eur      number    Calculé auto (cny / taux_rmb)
  prix_vip_eur        number    Prix négocié VIP
  options_payantes    array     [{ref, nom_fr, nom_zh, nom_en, surcout_eur}]
  dimensions          object    {l, L, h, volume_m3, poids_net_kg, poids_brut_kg}
  code_hs             string    "8429.52"
  fournisseur         string    "RIPPA"
  actif               boolean
  score_completude    number    0-100 calculé auto
  photos              string[]  URLs Firebase Storage
  video_url           string
  pdf_url             string
  createdAt           timestamp
  updatedAt           timestamp

/quotes/{id}
  numero              string    "DVS-2604001"
  client_id           string
  client_nom          string
  client_email        string
  client_tel          string
  client_adresse      string
  client_siret        string
  partenaire_id       string|null
  statut              string    brouillon|envoyé|accepté|refusé|annulé
  lignes              array     [{ref, nom_fr, qte, prix_unitaire, total}]
  total_ht            number
  acompte_pct         number    30
  acomptes            array     [{date, montant, ref_fa, pdf_url}]
  total_encaisse      number
  solde_restant       number
  destination         string    MQ|GP|RE|GF|FR
  createdAt           timestamp
  updatedAt           timestamp

/invoices/{id}
  numero              string    "FA-2604001" | "F-2604001"
  type                string    "acompte" | "facture"
  quote_id            string
  montant             number
  pdf_url             string
  createdAt           timestamp

/commissions/{id}
  numero              string    "NC-2604001"
  partenaire_id       string
  partenaire_nom      string
  lignes              array     [{quote_id, client, montant_ht, taux, commission}]
  total_commission    number
  statut              string    "en attente" | "payée"
  createdAt           timestamp

/containers/{id}
  numero              string    "CONT-2604-01"
  type                string    "20ft" | "40ft" | "40ft-HC"
  destination         string    MQ|GP|RE|GF|FR
  statut              string    préparation|chargé|parti|arrivé|livré
  date_depart         timestamp
  date_arrivee_prevue timestamp
  voyage_number       string
  bl_waybill          string
  seal                string
  port_chargement     string    "NINGBO"
  port_destination    string    "FORT-DE-FRANCE"
  lignes              array     [{ref, nom_fr, nom_zh, qte_colis, qte_pieces, l, L, h, volume_m3, poids_net}]
  volume_total        number    auto-calculé
  poids_total         number    auto-calculé
  createdAt           timestamp

/partners/{id}
  nom                 string
  code                string    "JM"|"TD"|"MC"|"AL"
  email               string
  tel                 string
  commission_taux     number    15
  actif               boolean

/sav/{id}
  numero              string    "SAV-2604001"
  client_id           string
  quote_id            string
  produit_ref         string
  description         string
  photos              string[]
  statut              string    nouveau|en cours|résolu|fermé
  piece_affectee      string
  createdAt           timestamp

/stock/{id}
  ref_piece           string
  nom                 string
  compatible          string[]
  qte                 number
  seuil_alerte        number

/admin_params/global
  taux_rmb_eur        number    7.82
  taux_rmb_updated    timestamp
  taux_majoration_user     number  2.0
  taux_majoration_partner  number  1.2
  acompte_pct_defaut  number    30
  delai_validite_devis number   30

/admin_params/emetteur
  nom                 "LUXENT LIMITED"
  adresse             "2ND FLOOR COLLEGE HOUSE, 17 KING EDWARDS ROAD"
  ville               "RUISLIP HA4 7AE — LONDON"
  pays                "UNITED KINGDOM"
  company_number      "14852122"
  email               "luxent@ltd-uk.eu"
  tel_cn              "+86 135 6627 1902"
  tel_fr              "+33 620 607 448"
  iban                "DE76 2022 0800 0059 5688 30"
  swift               "SXPYDEHH"
  banque              "Banking Circle S.A."

/counters/{PREFIX_AAMM}
  valeur              number    (incrémental atomique)

/logs/{id}
  action              string
  user                string
  details             object
  createdAt           timestamp
```

---

## 📁 STRUCTURE DES FICHIERS (from scratch)

```
97import-firebase/
├── .env.local                    ← clés API (jamais dans Git)
├── .gitignore
├── 97.md                         ← ce fichier
├── MAJALL.TXT                    ← log de toutes les actions
├── index.html
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
├── package.json
├── public/
│   ├── luxent.png                ← logo (transparent)
│   └── images/                   ← images produits {ref}-01.png
├── scripts/
│   ├── seed-users.js             ← création 5 comptes test
│   ├── seed-products.js          ← 5 produits test Firestore
│   └── upload-images.js          ← upload images → Storage
├── src/
│   ├── main.tsx
│   ├── App.tsx                   ← routing wouter (front + admin)
│   ├── i18n/
│   │   └── index.ts              ← FR/ZH/EN
│   ├── lib/
│   │   ├── firebase.ts           ← dual auth instances
│   │   ├── counters.ts           ← numérotation documents
│   │   ├── pdf-generator.ts      ← 5 templates PDF
│   │   ├── excel-generator.ts    ← BC/BE/BD Excel
│   │   ├── deepl.ts              ← traduction auto
│   │   └── exchange-rate.ts      ← taux RMB API
│   ├── components/
│   │   ├── GlobeToggle.tsx       ← 🌐 FR/ZH/EN
│   │   ├── DuplicateBtn.tsx      ← ⧉ bouton réutilisable
│   │   ├── OrangeIndicator.tsx   ← 🟠 pastille champ vide
│   │   ├── SortControl.tsx       ← tri récent/ancien
│   │   └── Tooltip.tsx           ← tooltip CSS custom
│   ├── admin/
│   │   ├── AdminApp.tsx          ← layout + sidebar
│   │   ├── AdminLogin.tsx
│   │   └── pages/
│   │       ├── Dashboard.tsx
│   │       ├── ListeDevis.tsx
│   │       ├── DetailDevis.tsx
│   │       ├── Factures.tsx
│   │       ├── NotesCommission.tsx
│   │       ├── FraisLogistique.tsx
│   │       ├── ListeConteneurs.tsx
│   │       ├── NouveauConteneur.tsx
│   │       ├── DetailConteneur.tsx
│   │       ├── Stock.tsx
│   │       ├── SAVListe.tsx
│   │       ├── SAVDetail.tsx
│   │       ├── CatalogueProduits.tsx
│   │       ├── NouveauProduit.tsx
│   │       ├── EditProduit.tsx    ← miroir EXACT de NouveauProduit
│   │       ├── Clients.tsx
│   │       ├── Partenaires.tsx
│   │       ├── TauxRMB.tsx
│   │       ├── Logs.tsx
│   │       └── Parametres.tsx
│   └── front/
│       ├── FrontApp.tsx
│       └── pages/
│           ├── Home.tsx
│           ├── Catalogue.tsx
│           ├── Produit.tsx
│           ├── Connexion.tsx
│           ├── Inscription.tsx
│           ├── Partenaire.tsx
│           ├── Panier.tsx
│           └── MonCompte/
│               ├── index.tsx
│               ├── Devis.tsx
│               ├── DetailDevis.tsx
│               ├── Factures.tsx
│               ├── Livraison.tsx
│               └── SAV.tsx
```

---

## 🔢 NUMÉROTATION DOCUMENTS

```typescript
// src/lib/counters.ts
import { doc, runTransaction, getFirestore } from 'firebase/firestore';
import { db } from './firebase';

export const getNextNumber = async (prefix: string): Promise<string> => {
  const now = new Date();
  const aa = String(now.getFullYear()).slice(-2);
  const mm = String(now.getMonth() + 1).padStart(2, '0');
  const aamm = `${aa}${mm}`;
  const counterId = `${prefix}_${aamm}`;
  const ref = doc(db, 'counters', counterId);
  const newVal = await runTransaction(db, async (tx) => {
    const snap = await tx.get(ref);
    const current = snap.exists() ? snap.data().valeur : 0;
    tx.set(ref, { valeur: current + 1 });
    return current + 1;
  });
  return `${prefix}-${aamm}${String(newVal).padStart(3, '0')}`;
};

// Préfixes : DVS | FA | F | NC | CONT | SAV | LA | STK
// Exemples : DVS-2604001 | FA-2604001 | NC-2604001
```

---

## 🖥️ PHASE A — BACK-OFFICE (admin.97import.com)

### A0 — Routing admin

```typescript
// src/App.tsx
import { Router, Route, Switch } from 'wouter';
import { I18nProvider } from './i18n';
import AdminApp from './admin/AdminApp';
import FrontApp from './front/FrontApp';

const isAdmin = window.location.hostname.startsWith('admin.');

export default function App() {
  return (
    <I18nProvider>
      {isAdmin ? <AdminApp /> : <FrontApp />}
    </I18nProvider>
  );
}
```

### A1 — Layout back-office

```typescript
// src/admin/AdminApp.tsx
// Sidebar navy #1E3A5F + navbar + globe 🌐 + routing 20 pages

const ADMIN_PAGES = [
  { path: '/admin',              label: 'nav.dashboard',    icon: '📊', component: Dashboard },
  { path: '/admin/devis',        label: 'nav.devis',        icon: '📋', component: ListeDevis },
  { path: '/admin/factures',     label: 'nav.factures',     icon: '🧾', component: Factures },
  { path: '/admin/commissions',  label: 'nav.commissions',  icon: '💰', component: NotesCommission },
  { path: '/admin/frais',        label: 'nav.frais',        icon: '🚢', component: FraisLogistique },
  { path: '/admin/conteneurs',   label: 'nav.conteneurs',   icon: '📦', component: ListeConteneurs },
  { path: '/admin/sav',          label: 'nav.sav',          icon: '🔧', component: SAVListe },
  { path: '/admin/stock',        label: 'nav.stock',        icon: '🗄️', component: Stock },
  { path: '/admin/produits',     label: 'nav.produits',     icon: '🛒', component: CatalogueProduits },
  { path: '/admin/clients',      label: 'nav.clients',      icon: '👥', component: Clients },
  { path: '/admin/partenaires',  label: 'nav.partenaires',  icon: '🤝', component: Partenaires },
  { path: '/admin/taux',         label: 'nav.taux',         icon: '💱', component: TauxRMB },
  { path: '/admin/analytics',    label: 'nav.analytics',    icon: '📈', component: Analytics },
  { path: '/admin/logs',         label: 'nav.logs',         icon: '📝', component: Logs },
  { path: '/admin/parametres',   label: 'nav.parametres',   icon: '⚙️', component: Parametres },
];
```

### A2 — Composants partagés obligatoires

**Tri par défaut = PLUS RÉCENT (toutes les listes) :**
```typescript
// src/components/SortControl.tsx
export const SortControl = ({ value, onChange }: {
  value: 'desc' | 'asc';
  onChange: (v: 'desc' | 'asc') => void;
}) => {
  const { t } = useI18n();
  return (
    <select value={value} onChange={e => onChange(e.target.value as any)}
      className="text-sm border rounded px-2 py-1">
      <option value="desc">{t('sort.recent')}</option>
      <option value="asc">{t('sort.oldest')}</option>
    </select>
  );
};
// Initialisation : useState<'desc'|'asc'>('desc')  ← toujours 'desc' par défaut
// Query : orderBy('createdAt', sortOrder)
```

**Bouton Dupliquer ⧉ :**
```typescript
// src/components/DuplicateBtn.tsx
export const DuplicateBtn = ({ onClick }: { onClick: () => void }) => {
  const { t } = useI18n();
  return (
    <div className="tooltip-wrapper">
      <button onClick={onClick}
        className="w-8 h-8 rounded-full border border-gray-300 flex items-center justify-center hover:bg-orange-50 text-gray-500">
        ⧉
      </button>
      <span className="tooltip-text">{t('btn.dupliquer')}</span>
    </div>
  );
};

// Logique de duplication (générique) :
export const duplicateDoc = async (data: any, collectionName: string, prefix: string) => {
  const { addDoc, collection, serverTimestamp } = await import('firebase/firestore');
  const { db } = await import('../lib/firebase');
  const newNum = await getNextNumber(prefix);
  const copy = { ...data, numero: newNum, statut: 'brouillon',
    nom: data.nom ? `${data.nom} (copie)` : undefined,
    createdAt: serverTimestamp(), updatedAt: serverTimestamp() };
  delete copy.id;
  return addDoc(collection(db, collectionName), copy);
};
```

**Pastille orange 🟠 :**
```typescript
// src/components/OrangeIndicator.tsx
export const OrangeIndicator = ({ show }: { show: boolean }) =>
  show ? <span className="inline-block w-2 h-2 rounded-full bg-orange-500 ml-1" title="Champ manquant" /> : null;

// Score complétude produit :
const REQUIRED = ['categorie','prix_achat_cny','nom_fr','nom_zh','nom_en',
  'description_fr','code_hs','poids_net_kg','photos'];
export const scoreCompletude = (p: any): number => {
  const filled = REQUIRED.filter(k => {
    const v = p[k];
    if (Array.isArray(v)) return v.length > 0;
    return v !== undefined && v !== null && v !== '' && v !== 0;
  });
  return Math.round(filled.length / REQUIRED.length * 100);
};
```

**Tooltip CSS custom (jamais title= natif) :**
```css
/* Dans tailwind.config.js : ajouter les classes custom */
.tooltip-wrapper { position: relative; display: inline-block; }
.tooltip-text {
  display: none; position: absolute;
  bottom: calc(100% + 6px); left: 50%;
  transform: translateX(-50%);
  background: #1a1a2e; color: white;
  padding: 4px 10px; border-radius: 4px;
  font-size: 11px; white-space: nowrap; z-index: 50;
}
.tooltip-wrapper:hover .tooltip-text { display: block; }
/* Si débordement haut → repositionner en bas automatiquement via JS */
```

**Globe 🌐 trilingue :**
```typescript
// src/components/GlobeToggle.tsx
export const GlobeToggle = () => {
  const { lang, setLang } = useI18n();
  return (
    <div className="flex items-center gap-1 text-sm">
      <span>🌐</span>
      {(['fr','zh','en'] as Lang[]).map(l => (
        <button key={l} onClick={() => setLang(l)}
          className={`px-2 py-0.5 rounded ${lang === l ? 'bg-navy text-white' : 'text-gray-500'}`}>
          {l === 'fr' ? 'FR' : l === 'zh' ? '中文' : 'EN'}
        </button>
      ))}
    </div>
  );
};
```

### A3 — Catalogue Produits

**RÈGLE ABSOLUE : `EditProduit.tsx` = miroir EXACT de `NouveauProduit.tsx`**
Même composant de formulaire `ProductForm.tsx` utilisé dans les deux pages.

```typescript
// src/admin/pages/NouveauProduit.tsx
// src/admin/pages/EditProduit.tsx
// Les deux utilisent : <ProductForm mode="new"|"edit" initialData={...} />

// src/admin/components/ProductForm.tsx
const BLOCS = {
  bdd: [
    { key: 'numero_interne', label: 'N° Interne', type: 'text', required: true },
    { key: 'categorie', label: 'Catégorie', type: 'select',
      options: ['mini-pelles','maisons-modulaires','solaire','machines-agricoles','divers','services'],
      required: true },
    { key: 'sous_categorie', label: 'Sous-catégorie', type: 'text', required: false },
    { key: 'fournisseur', label: 'Fournisseur', type: 'text', required: false },
    { key: 'prix_achat_cny', label: 'Prix achat CNY ¥', type: 'number', required: true },
    { key: 'prix_achat_eur', label: 'Prix achat EUR €', type: 'computed', required: false },
    { key: 'dimensions.l', label: 'Longueur (cm)', type: 'number', required: true },
    { key: 'dimensions.L', label: 'Largeur (cm)', type: 'number', required: true },
    { key: 'dimensions.h', label: 'Hauteur (cm)', type: 'number', required: true },
    { key: 'dimensions.volume_m3', label: 'Volume m³', type: 'computed', required: false },
    { key: 'dimensions.poids_net_kg', label: 'Poids net kg', type: 'number', required: true },
    { key: 'dimensions.poids_brut_kg', label: 'Poids brut kg', type: 'number', required: false },
    { key: 'code_hs', label: 'Code HS douanier', type: 'text', required: true },
  ],
  web: [
    { key: 'nom_fr', label: 'Nom FR', type: 'text', required: true },
    { key: 'nom_zh', label: 'Nom ZH 中文', type: 'text+deepl', required: true },
    { key: 'nom_en', label: 'Nom EN', type: 'text+deepl', required: true },
    { key: 'description_fr', label: 'Description FR', type: 'textarea', required: true },
    { key: 'description_zh', label: 'Description ZH', type: 'textarea+deepl', required: true },
    { key: 'description_en', label: 'Description EN', type: 'textarea+deepl', required: true },
    { key: 'photos', label: 'Photos', type: 'upload', required: true },
    { key: 'video_url', label: 'Vidéo URL', type: 'text', required: false },
    { key: 'pdf_url', label: 'PDF fiche technique', type: 'upload', required: false },
    { key: 'actif', label: 'Actif sur le site', type: 'toggle', required: false },
  ],
};

// Sauvegarde auto debounce 800ms sur chaque changement
// → updateDoc(doc(db,'products',id), { [key]: value, updatedAt: serverTimestamp() })
// → toast "Sauvegardé ✓" 2 secondes
```

### A4 — Taux RMB

```typescript
// src/lib/exchange-rate.ts
const KEY = import.meta.env.VITE_EXCHANGE_KEY; // 51273802828592038c41e0f1

export const fetchRMBRate = async (): Promise<number> => {
  const res = await fetch(`https://v6.exchangerate-api.com/v6/${KEY}/pair/EUR/CNY`);
  const data = await res.json();
  if (data.result !== 'success') throw new Error(data['error-type']);
  return data.conversion_rate;
};

// Page TauxRMB :
// - Affiche 1 EUR = X.XX CNY (taux API)
// - Taux avec marge +2% = taux × 1.02 (affiché en orange)
// - Ce taux majoré est utilisé pour tous les calculs prix produit
// - Bouton "Actualiser maintenant"
// - Sauvegarde dans Firestore /admin_params/global.taux_rmb_eur
// - Auto-refresh toutes les 24h (Cloud Function scheduled)
// - Historique des 30 derniers taux (tableau simple)
```

### A5 — Génération PDF

```typescript
// src/lib/pdf-generator.ts
// Utilise jsPDF
// Design IMMUABLE :
//   Logo LUXENT en-tête droite (base64 depuis public/luxent.png)
//   Titres couleur #C87F6B
//   Fond entête tableau #FBF0ED
//   Bordures #E5E5E5
//   Police Arial
//   PAS de fond sombre — PAS de texte blanc — PAS de bordures noires

// 5 templates :

// 1. generateDevis(quote) → DVS-2604001.pdf
//    Émetteur LUXENT + Destinataire client
//    Tableau : Réf | Désignation | Qté | PU HT | Total HT
//    Totaux + acompte 30% demandé
//    Validité 30j + IBAN Banking Circle

// 2. generateFactureAcompte(quote, acompte) → FA-2604001.pdf
//    Lien devis source + montant acompte + solde restant

// 3. generateFactureFinale(quote) → F-2604001.pdf
//    Toutes lignes + déduction acomptes + total final

// 4. generateNoteCommission(commission) → NC-2604001.pdf
//    Partenaire + lignes devis + commission par ligne + total

// 5. generateBDInvoice(container) → BD-F-2604001.pdf
//    Trilingue EN/FR/ZH — colonnes HS Code — prix USD
//    Infos conteneur : port chargement/destination, BL, SEAL
```

### A6 — Génération Excel

```typescript
// src/lib/excel-generator.ts
// Utilise SheetJS (xlsx)
// Reprendre EXACTEMENT la structure des templates uploadés

// generateBCChine(container) :
// Ligne 1 : "BC CHINE 中国采购单 — LISTE D'ACHAT MICHEL — 97IMPORT.COM"
// Ligne 2 : Date | Conteneur | Destination | calcul volume auto
// Ligne 3 : Totaux (lignes | colis | pièces | volume m³ | poids net | poids brut)
// Ligne 4 : En-têtes EN/ZH (N° | N° Colis | N° Interne | Nom FR | Nom ZH | Qté | L | l | H | Volume | Poids Net...)
// Ligne 5+ : Données
// Formule volume auto : =L×l×H/1000000

// generateBEExport(container) :
// Même structure — colonnes douane chinoise
// Titre : "BE EXPORT 出口单 — DOUANE CHINOISE — 97IMPORT.COM"

// generateBDPackingList(container) :
// Template PACKAGING LIST / 装箱单 / LISTE DE COLISAGE
// Entête LUXENT LIMITED trilingue

// generateBDInvoiceExcel(container) :
// Template INVOICE / 发票 / FACTURE
// Entête LUXENT LIMITED trilingue
```

### A7 — Module Conteneurs

```typescript
// Actions disponibles sur un conteneur :
// ⧉ Dupliquer      → même type/destination, lignes vides, nouveau numéro
// 📥 BC CHINE      → generateBCChine(container) → download Excel
// 📥 BE EXPORT     → generateBEExport(container) → download Excel
// 📥 BD INVOICE    → generateBDInvoiceExcel(container) → download Excel
// 📥 BD PACKINGLIST → generateBDPackingList(container) → download Excel
// 📄 BD Invoice PDF → generateBDInvoice(container) → download PDF
// 🚢 Parti du port  → update statut → log horodaté Europe/Paris

// Auto-calcul volume total :
// volume_total = sum(lignes.map(l => l.l * l.L * l.h / 1000000))
// poids_total  = sum(lignes.map(l => l.poids_net))
```

### A8 — Système Acomptes

```typescript
// Bouton ENCAISSER dans DetailDevis :
// 1. Modal : saisir montant (€) + date paiement
// 2. getNextNumber('FA') → "FA-2604001"
// 3. generateFactureAcompte() → PDF
// 4. Upload PDF → Firebase Storage /invoices/FA-2604001.pdf
// 5. addDoc /invoices → {numero, type:'acompte', quote_id, montant, pdf_url}
// 6. updateDoc /quotes/{id} :
//    acomptes: arrayUnion({date, montant, ref_fa, pdf_url})
//    total_encaisse: total_encaisse + montant
//    solde_restant: total_ht - (total_encaisse + montant)
// 7. Log action dans /logs
// 8. Toast "Acompte enregistré ✓"
```

### A9 — DeepL Traduction Auto

```typescript
// src/lib/deepl.ts
const KEY = import.meta.env.VITE_DEEPL_KEY; // 3fae7c40-bed9-48ee-88d9-26c5f719caf3:fx
const URL = 'https://api-free.deepl.com/v2/translate';

export type DeepLLang = 'FR' | 'ZH' | 'EN-GB';

export const translateText = async (text: string, target: DeepLLang): Promise<string> => {
  const res = await fetch(URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json',
      'Authorization': `DeepL-Auth-Key ${KEY}` },
    body: JSON.stringify({ text: [text], target_lang: target })
  });
  const data = await res.json();
  return data.translations[0].text;
};

// Usage dans ProductForm :
// Quand nom_fr change → bouton "Traduire" → translateText(nom_fr, 'ZH') + translateText(nom_fr, 'EN-GB')
// Même chose pour description_fr → description_zh + description_en
```

---

## 🌐 PHASE B — FRONT-OFFICE (www.97import.com)

### B1 — Homepage

```
Header : Logo 97import.com + nav + 🌐 globe + bouton Connexion
Hero : Titre FR/ZH/EN + sous-titre DOM-TOM + CTA "Voir le catalogue"
Catégories : 6 cards (Mini-Pelles / Maisons / Solaire / Machines / Divers / Services)
Search bar : centrée, recherche par nom ou ref
Photos réelles depuis Firebase Storage
Footer : LUXENT LIMITED + contacts + liens légaux
```

### B2 — Catalogue & Fiche produit

```typescript
// Affichage prix selon rôle Firebase Custom Claims :
const getDisplayPrice = (product: any, role: string, tauxRmb: number) => {
  const eur = product.prix_achat_cny / tauxRmb;
  switch (role) {
    case 'user':    return eur * 2;
    case 'partner': return eur * 1.2;
    case 'vip':     return product.prix_vip_eur ?? eur * 1.5;
    case 'admin':   return eur;
    default:        return null; // visiteur → pas de prix
  }
};

// Tableau par rôle :
// Visiteur  : photos ✅ | description ✅ | prix ❌ | panier → login
// User      : photos ✅ | description ✅ | prix × 2 | panier ✅
// VIP       : photos ✅ | description ✅ | prix négocié | panier ✅
// Partner   : photos ✅ | description ✅ | prix × 1.2 | panier ✅
// Admin     : photos ✅ | description ✅ | prix achat ✅ | panier ✅
```

### B3 — Panier & Devis

```typescript
// Flux complet :
// 1. Ajout produit au panier (localStorage)
// 2. Clic "Demander un devis"
// 3. Si non connecté → /connexion (redirect back après login)
// 4. Sélection destination (MQ | GP | RE | GF | FR)
// 5. Formulaire client (nom, tel, adresse, SIRET)
// 6. Création Firestore : getNextNumber('DVS') → {numero, statut:'brouillon', lignes, client_id...}
// 7. Email confirmation client (Resend API via Cloud Function)
// 8. Redirect → /mon-compte/devis/:id
// PAS de commande directe — uniquement devis → admin valide
```

### B4 — Espace client (documents progressifs)

```
/mon-compte/devis/:id — affichage selon statut :
  brouillon → Aperçu grisé (non téléchargeable)
  envoyé    → PDF devis téléchargeable
  accepté   → + Facture acompte
  en cours  → + Suivi conteneur (statut + date estimée arrivée)
  expédié   → + BD Invoice PDF + Packing List
  livré     → + Facture finale

Pas d'onglet "Commandes" dans l'espace client.
```

---

## 🌱 PHASE C — SEED BASE DE DONNÉES

### C1 — 5 produits test (1 par catégorie)

```javascript
// scripts/seed-products.js
const products = [
  {
    numero_interne: 'MP-R22-001',
    categorie: 'mini-pelles',
    nom_fr: 'Mini-pelle R22 — chenille caoutchouc',
    nom_zh: '挖掘机 R22 — 橡胶履带',
    nom_en: 'Mini Excavator R22 — Rubber Track',
    prix_achat_cny: 34200,
    dimensions: { l: 315, L: 125, h: 255, volume_m3: 10.07, poids_net_kg: 2200, poids_brut_kg: 2350 },
    code_hs: '8429.52', fournisseur: 'RIPPA', actif: true,
    options_payantes: [
      { ref: 'MP-R22-001-MET', nom_fr: 'Chenille métal', nom_zh: '钢履带', nom_en: 'Metal Track', surcout_eur: 450 },
      { ref: 'ACC-GD-030', nom_fr: 'Godet 30cm', nom_zh: '30cm斗', nom_en: '30cm Bucket', surcout_eur: 320 },
    ]
  },
  {
    numero_interne: 'MS-20-001',
    categorie: 'maisons-modulaires',
    nom_fr: 'Maison Standard 20m²',
    nom_zh: '标准集装箱房 20平米',
    nom_en: 'Standard Modular Home 20sqm',
    prix_achat_cny: 28000, actif: true
  },
  {
    numero_interne: 'SOL-KIT-001',
    categorie: 'solaire',
    nom_fr: 'Kit solaire 3kWc complet',
    nom_zh: '太阳能套件 3kWc',
    nom_en: 'Solar Kit 3kWc Complete',
    prix_achat_cny: 8500, actif: true
  },
  {
    numero_interne: 'MA-MOTO-001',
    categorie: 'machines-agricoles',
    nom_fr: 'Motoculteur 7CV',
    nom_zh: '耕地机 7马力',
    nom_en: 'Power Tiller 7HP',
    prix_achat_cny: 3200, actif: true
  },
  {
    numero_interne: 'LOG-FRET-MQ-001',
    categorie: 'services',
    nom_fr: 'Fret maritime — Martinique (20ft)',
    nom_zh: '海运费 — 马提尼克 (20英尺)',
    nom_en: 'Sea Freight — Martinique (20ft)',
    prix_achat_cny: 9600, actif: true
  }
];
```

### C2 — Paramètres initiaux

```javascript
// /admin_params/global
{ taux_rmb_eur: 7.82, taux_rmb_updated: new Date(),
  taux_majoration_user: 2.0, taux_majoration_partner: 1.2,
  acompte_pct_defaut: 30, delai_validite_devis: 30 }

// /admin_params/emetteur
{ nom: 'LUXENT LIMITED',
  adresse: '2ND FLOOR COLLEGE HOUSE, 17 KING EDWARDS ROAD',
  ville: 'RUISLIP HA4 7AE — LONDON', pays: 'UNITED KINGDOM',
  company_number: '14852122', email: 'luxent@ltd-uk.eu',
  tel_cn: '+86 135 6627 1902', tel_fr: '+33 620 607 448',
  iban: 'DE76 2022 0800 0059 5688 30',
  swift: 'SXPYDEHH', banque: 'Banking Circle S.A.' }

// /partners — 4 partenaires
// JM : Jean-Marc | TD : Thomas Dupont | MC : Michel Chen | AL : Alice Laurent
// commission_taux: 15, actif: true
```

### C3 — Upload images produits

```javascript
// scripts/upload-images.js
// Source : /Users/mactell/97import2026_siteweb/vercel/images/
// Convention : {numero_interne}-01.png
// Destination : Firebase Storage /products/{numero_interne}/01.png
// Après upload → updateDoc Firestore : photos: [downloadURL]

const { createReadStream, readdirSync } = require('fs');
const path = require('path');
const IMG_DIR = '/Users/mactell/97import2026_siteweb/vercel/images';

// Pour chaque fichier {ref}-01.png :
// 1. Upload vers Storage
// 2. Obtenir downloadURL
// 3. Trouver le produit dans Firestore par numero_interne == ref
// 4. updateDoc → photos: [downloadURL]
```

---

## 📋 RÈGLES GIT OBLIGATOIRES

```bash
# Avant chaque phase :
git fetch origin && git reset --hard origin/main
git tag backup-$(date +%Y%m%d-%H%M) && git push origin --tags
git log --oneline -3

# Après chaque phase :
git add -A && git commit -m "Phase [X] — [description]" && git push origin main
```

---

## 📝 FORMAT MAJALL.TXT

```
[2026-04-11 10:00] INIT — Projet créé from scratch, dépendances installées
[2026-04-11 10:15] Phase A1 — Layout admin + sidebar 15 entrées opérationnel
[2026-04-11 10:30] Phase A2 — Composants partagés : SortControl, DuplicateBtn, OrangeIndicator, GlobeToggle
[2026-04-11 10:45] Phase A3 — ProductForm miroir new/edit + pastilles oranges + sauvegarde auto
[2026-04-11 11:00] ERREUR — [description] → CORRECTION : [solution appliquée]
```

---

## ⚠️ ANTI-DUPLICATION (vérification obligatoire avant chaque fichier)

```bash
# Vérifier avant de créer un composant :
find src/ -name "*.tsx" -o -name "*.ts" | xargs grep -l "NomDuComposant" 2>/dev/null
# Si trouvé → utiliser l'existant, ne pas créer de doublon
```

---

## 🚀 ORDRE D'EXÉCUTION (20 étapes séquentielles)

```
01. Supprimer ancien code (rm -rf src/ etc.)
02. npm create vite + npm install toutes dépendances
03. Créer .env.local avec toutes les clés API
04. src/lib/firebase.ts (dual auth)
05. src/i18n/index.ts (FR/ZH/EN complet)
06. src/lib/counters.ts (numérotation atomique)
07. src/components/ (GlobeToggle, DuplicateBtn, OrangeIndicator, SortControl, Tooltip)
08. scripts/seed-users.js → exécuter → 5 comptes créés Firebase
09. Phase A1 : AdminApp.tsx + Sidebar + Navbar + routing
10. Phase A3 : ProductForm + NouveauProduit + EditProduit (miroir)
11. Phase C : seed-products.js → 5 produits Firestore + params + partenaires
12. Phase A4 : TauxRMB + exchange-rate.ts
13. Phase A2 : ListeDevis + DetailDevis + Factures + Acomptes
14. Phase A5 : pdf-generator.ts (5 templates)
15. Phase A6 : excel-generator.ts (BC/BE/BD) + ListeConteneurs + DetailConteneur
16. Phase A7-A9 : SAV + Stock + Partenaires + NC + Dashboard + Logs + Parametres
17. Phase B : FrontApp + Home + Catalogue + Produit + Panier + MonCompte
18. scripts/upload-images.js → images Firebase Storage
19. npm run build → test production
20. Deploy Vercel : www.97import.com + admin.97import.com
```

---

## ✅ CHECKLIST FINALE

- [ ] FROM SCRATCH — aucun ancien code réutilisé
- [ ] .env.local créé avec toutes les clés (jamais dans Git)
- [ ] Système i18n FR/ZH/EN — zéro texte en dur
- [ ] Dual Firebase Auth (client + admin isolés)
- [ ] 5 comptes test créés (20262026)
- [ ] 5 produits test Firestore (1 par catégorie)
- [ ] Taux RMB API opérationnel + marge +2%
- [ ] DeepL traduction auto FR→ZH→EN
- [ ] Tri "plus récent" par défaut sur toutes les listes
- [ ] Bouton ⧉ Dupliquer sur toutes les listes
- [ ] EditProduit = miroir exact NouveauProduit
- [ ] Pastilles 🟠 sur tous les champs vides
- [ ] Sauvegarde auto Firestore (debounce 800ms)
- [ ] Tooltips CSS custom (jamais title= natif)
- [ ] Globe 🌐 FR/ZH/EN dans navbar (admin + front)
- [ ] 5 templates PDF (DVS, FA, F, NC, BD Invoice)
- [ ] 4 Excel (BC CHINE, BE EXPORT, BD Invoice, BD Packing)
- [ ] Images uploadées depuis vercel/images/ → Storage
- [ ] 2 sous-domaines Vercel déployés
- [ ] MAJALL.TXT à jour
- [ ] git push final sur origin/main
